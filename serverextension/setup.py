import setuptools

setuptools.setup(
    name="simple_server_extension",
    version="0.0.1",
    packages=setuptools.find_packages(),
    license="MIT",
    install_requires=["notebook", "packaging"],
)

